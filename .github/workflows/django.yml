name: Orange County Lettings

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ 3.12 ]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Lint with flake8
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=99 --statistics
      - name: Run Tests
        run: |
          coverage run -m pytest
          coverage xml --fail-under=80

      - name: Run docker
        run: |
          docker build -t featherinefall/hha-ocr-p13:$GITHUB_SHA .
          echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ vars.DOCKER_USERNAME }} --password-stdin
          docker push featherinefall/hha-ocr-p13:$COMMIT_HASH
        
        


